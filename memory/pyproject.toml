[project]
name = "materialhack-memory"
version = "0.1.0"
description = "Versioned memory abstraction for agentic protein design loops."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
turingdb = ["turingdb>=1.29"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
